DE_unified_LUSC.r script perform the DEA using limma package and all the results are included in the limma folder.
Before normalization and filtering steps, only protein-coding genes were retrieved (using biomaRt package).

Data after the normalization and filtering steps were saved as "unifiedData_normalized_luad.RData" and "unifiedData_normalized_filtered_luad.RData", respectively.

